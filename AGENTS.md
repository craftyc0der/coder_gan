# Project Instructions for Codex

## Scope

- NEVER read or access any code inside the `orchestrator/src` folder.
- If a task requires information from `orchestrator/src`, ask the user to provide the relevant snippets instead.

## Enforcement

- Do not open, search, or reference files under `orchestrator/src`.
- If a request conflicts with this rule, explain the constraint and request clarification or alternative inputs.

## Test file location

- All test files are located in `orchestrator/tests/`.
- Do not access or reference any files in `orchestrator/src/` for test-related tasks.
- You will be given the necessary code snippets from `orchestrator/src/` if needed for testing purposes.